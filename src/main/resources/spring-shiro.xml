<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!--与web.xml文件里的shiroFilter过滤器里的targetBeanName相对应-->
    <bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">
        <property name="securityManager" ref="securityManager"></property>
        <!--登陆页面-->
        <!--<property name="loginUrl" value="/login"></property>-->
        <!--没有权限跳转到的页面-->
        <!--<property name="unauthorizedUrl" value="/refuse"></property>-->
        <property name="filters">
            <map>
                <entry key="authc" value-ref="authenticationFilter"></entry>
                <entry key="logout" value-ref="logoutFilter"></entry>
            </map>
        </property>
        <!-- 过滤器链定义，从上向下顺序执行，一般将/**放在最下边 -->
        <property name="filterChainDefinitions">
            <value>
                <!-- 对静态资源设置匿名访问 -->
                /images/** = anon
                /js/** = anon
                /styles/** = anon
                <!--注册用户可匿名访问-->
                /register/** = anon
                <!-- 请求 logout.action地址，shiro去清除session-->
                /logout = logout
                <!-- = authc 所有url都必须认证通过才可以访问-->
                /** = authc
            </value>
        </property>
    </bean>

    <!-- securityManager安全管理器 -->
    <bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
        <property name="realm" ref="realm"></property>
        <property name="cacheManager" ref="shiroCacheManager"></property>
        <property name="sessionManager" ref="shiroSessionManager"></property>
    </bean>

    <!--自定义Realm-->
    <bean id="realm" class="com.meng.chatonline.security.MyRealm">
        <property name="credentialsMatcher" ref="credentialsMatcher"></property>
    </bean>

    <!--凭证匹配器-->
    <bean id="credentialsMatcher" class="org.apache.shiro.authc.credential.HashedCredentialsMatcher">
        <property name="hashAlgorithmName" value="md5"></property>
        <property name="hashIterations" value="2"></property>
    </bean>

    <!-- 自定义form认证过滤器 -->
    <!-- 基于Form表单的身份验证过滤器，不配置将也会注册此过虑器，表单中的用户账号、密码及loginurl将采用默认值，
        建议配置 -->
    <bean id="authenticationFilter" class="com.meng.chatonline.security.MyFormAuthenticationFilter">
        <property name="usernameParam" value="account"></property>
        <property name="passwordParam" value="password"></property>
        <!--认证成功后跳转的页面，默认认证成功自动跳转到上一个请求路径-->
        <property name="successUrl" value="/chatRoom"></property>
        <property name="loginUrl" value="/login"></property>

        <!--设置是否一个用户只允许在一处登陆-->
        <property name="onlyOnePermitLogin" value="true"></property>
    </bean>

    <!--自定义注销退出过滤器，用于用户注销时向其他用户广播注销消息-->
    <bean id="logoutFilter" class="com.meng.chatonline.security.MyLogoutFilter"></bean>

    <!--缓存管理器-->
    <bean id="shiroCacheManager" class="org.apache.shiro.cache.ehcache.EhCacheManager">
        <property name="cacheManager" ref="ehCacheManager"></property>
        <!--<property name="cacheManagerConfigFile" value="classpath:ehcache.xml"></property>-->
    </bean>

    <!-- 会话ID生成器 -->
    <bean id="sessionIdGenerator" class="org.apache.shiro.session.mgt.eis.JavaUuidSessionIdGenerator"/>
    <bean id="sessionDAO" class="org.apache.shiro.session.mgt.eis.EnterpriseCacheSessionDAO">
         <property name="sessionIdGenerator" ref="sessionIdGenerator"/>
    </bean>
    <!-- <bean id="sessionIdGenerator" class="com.concom.security.infrastructure.shiro.ShiroSessionIdGenerator"/> -->

    <bean id="shiroSessionManager" class="org.apache.shiro.web.session.mgt.DefaultWebSessionManager">
        <property name="sessionDAO" ref="sessionDAO"/>
        <!-- 删除失效的session -->
        <property name="deleteInvalidSessions" value="true"/>
        <!-- 相隔多久检查一次session的有效性 -->
        <property name="sessionValidationInterval" value="1800000"/>
        <!-- session 默认有效时间为半小时 （毫秒单位）-->
        <!--<property name="globalSessionTimeout" value="1800000"/>-->

        <!--配置自定义session监听器-->
        <property name ="sessionListeners">
            <list>
                <ref bean="sessionListener1"></ref>
            </list>
        </property>
    </bean>
    <bean id="sessionListener1" class="com.meng.chatonline.security.MySessionListener"></bean>
</beans>